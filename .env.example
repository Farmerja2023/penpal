# Live Issuing Configuration (.env.example)
# Copy to .env and populate with your Stripe credentials to enable live issuing

# ===== REQUIRED FOR LIVE MODE =====

# Repository-wide flag: must be set to "1" or "true" to enable any live operations
ENABLE_LIVE_MODE=0

# Your Stripe secret API key (starts with sk_live_... for production)
# Obtain from https://dashboard.stripe.com/apikeys
STRIPE_API_KEY=sk_live_your_key_here

# Per-run live issuing flag (used together with ENABLE_LIVE_MODE)
STRIPE_LIVE=0

# ===== OPTIONAL: FOR TOP-UPS =====

# Enable actual top-up operations (moves real money when ENABLE_LIVE_MODE=1)
STRIPE_DO_TOPUP=0

# Amount to top-up in cents (default 1000 = $10.00)
STRIPE_TOPUP_AMOUNT_CENTS=1000

# ===== RECONCILIATION =====

# Optional: path to SQLite database for reconciliation state
# RECONCILIATION_DB_PATH=./reconciliation.db
